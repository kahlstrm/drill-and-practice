<% if (it.errors && it.errors[it.attribute]) { %>
<ul
  class="bg-warning text-danger mt-2 mb-2 border-dark rounded list-unstyled fw-bold"
>
  <% Object.values(it.errors[it.attribute]).forEach((err) => { %>
  <li class="p-1"><%= err %></li>
  <% }); %>
</ul>
<% } %>
