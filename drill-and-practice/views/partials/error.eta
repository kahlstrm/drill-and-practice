<% if (it.errors && it.errors[it.attribute]) { %>
<ul class="bg-warning text-danger">
    <% Object.values(it.errors[it.attribute]).forEach((err) => { %>
        <li ><%= err %></li>
    <% }); %>
</ul>
<% } %>